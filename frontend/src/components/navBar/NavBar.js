import React from "react";
import styles from "./navBar.module.css";

const NavBar = (props) => {
  return (
    <>
      <header className={styles.navBar}> Barra navegacion</header>
    </>
  );
};

export default NavBar;
